# Demo Playwright suite

Shared browser tests for interactive demos.

## Setup

```bash
cd demo
npm install
npm run install-browsers
```

## Run

```bash
cd demo
npm test
```

Covers:

- `trainer`
- `chord_progressions`
- `running_entropy`
- `scenes/kompone/kompone`
